<!DOCTYPE html>
<% include header  %>

<div class="container">
<div class="section">
<div class="row">
    <h1 class="center">Connexion</h1>
    <form action="/contrat" method="POST" class="col s12">
      <div class="row">
        <div class="input-field col s12">
          <input placeholder="Password" id="password" type="password" class="validate" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" required>
        </div>
      </div>
      <div class="row">
            <div class="center input-field col s12">
                <button class="btn waves-effect waves-light" type="submit" name="action">Validez</button>
            </div>
        </div>
    </form>
  </div>
  </div>
  </div>

<% include footer.ejs  %>